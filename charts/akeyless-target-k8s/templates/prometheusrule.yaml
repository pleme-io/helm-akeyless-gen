{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-target-k8s.fullname" . }}
  labels:
    {{- include "akeyless-target-k8s.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-target-k8s.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-target-k8s.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-target-k8s.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-target-k8s.fullname" . }} is down"
            description: "{{ include "akeyless-target-k8s.fullname" . }} has been down for more than 2 minutes."
{{- end }}
