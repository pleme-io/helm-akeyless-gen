{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-esm.fullname" . }}
  labels:
    {{- include "akeyless-esm.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-esm.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-esm.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-esm.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-esm.fullname" . }} is down"
            description: "{{ include "akeyless-esm.fullname" . }} has been down for more than 2 minutes."
{{- end }}
