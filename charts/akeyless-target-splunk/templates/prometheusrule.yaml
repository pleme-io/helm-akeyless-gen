{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-target-splunk.fullname" . }}
  labels:
    {{- include "akeyless-target-splunk.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-target-splunk.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-target-splunk.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-target-splunk.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-target-splunk.fullname" . }} is down"
            description: "{{ include "akeyless-target-splunk.fullname" . }} has been down for more than 2 minutes."
{{- end }}
