{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-target-globalsign-atlas.fullname" . }}
  labels:
    {{- include "akeyless-target-globalsign-atlas.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-target-globalsign-atlas.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-target-globalsign-atlas.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-target-globalsign-atlas.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-target-globalsign-atlas.fullname" . }} is down"
            description: "{{ include "akeyless-target-globalsign-atlas.fullname" . }} has been down for more than 2 minutes."
{{- end }}
