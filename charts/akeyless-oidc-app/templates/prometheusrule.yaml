{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-oidc-app.fullname" . }}
  labels:
    {{- include "akeyless-oidc-app.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-oidc-app.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-oidc-app.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-oidc-app.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-oidc-app.fullname" . }} is down"
            description: "{{ include "akeyless-oidc-app.fullname" . }} has been down for more than 2 minutes."
{{- end }}
