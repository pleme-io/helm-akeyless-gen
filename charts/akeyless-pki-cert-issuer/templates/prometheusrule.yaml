{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-pki-cert-issuer.fullname" . }}
  labels:
    {{- include "akeyless-pki-cert-issuer.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-pki-cert-issuer.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-pki-cert-issuer.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-pki-cert-issuer.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-pki-cert-issuer.fullname" . }} is down"
            description: "{{ include "akeyless-pki-cert-issuer.fullname" . }} has been down for more than 2 minutes."
{{- end }}
