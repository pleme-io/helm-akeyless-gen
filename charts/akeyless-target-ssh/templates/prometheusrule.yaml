{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-target-ssh.fullname" . }}
  labels:
    {{- include "akeyless-target-ssh.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-target-ssh.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-target-ssh.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-target-ssh.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-target-ssh.fullname" . }} is down"
            description: "{{ include "akeyless-target-ssh.fullname" . }} has been down for more than 2 minutes."
{{- end }}
