{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-tokenizer.fullname" . }}
  labels:
    {{- include "akeyless-tokenizer.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-tokenizer.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-tokenizer.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-tokenizer.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-tokenizer.fullname" . }} is down"
            description: "{{ include "akeyless-tokenizer.fullname" . }} has been down for more than 2 minutes."
{{- end }}
