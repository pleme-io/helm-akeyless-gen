{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-target-sectigo.fullname" . }}
  labels:
    {{- include "akeyless-target-sectigo.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-target-sectigo.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-target-sectigo.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-target-sectigo.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-target-sectigo.fullname" . }} is down"
            description: "{{ include "akeyless-target-sectigo.fullname" . }} has been down for more than 2 minutes."
{{- end }}
