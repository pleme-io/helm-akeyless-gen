{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-account-custom-field.fullname" . }}
  labels:
    {{- include "akeyless-account-custom-field.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-account-custom-field.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-account-custom-field.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-account-custom-field.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-account-custom-field.fullname" . }} is down"
            description: "{{ include "akeyless-account-custom-field.fullname" . }} has been down for more than 2 minutes."
{{- end }}
