{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-target-gitlab.fullname" . }}
  labels:
    {{- include "akeyless-target-gitlab.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-target-gitlab.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-target-gitlab.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-target-gitlab.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-target-gitlab.fullname" . }} is down"
            description: "{{ include "akeyless-target-gitlab.fullname" . }} has been down for more than 2 minutes."
{{- end }}
