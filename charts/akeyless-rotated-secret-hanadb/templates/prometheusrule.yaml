{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-rotated-secret-hanadb.fullname" . }}
  labels:
    {{- include "akeyless-rotated-secret-hanadb.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-rotated-secret-hanadb.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-rotated-secret-hanadb.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-rotated-secret-hanadb.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-rotated-secret-hanadb.fullname" . }} is down"
            description: "{{ include "akeyless-rotated-secret-hanadb.fullname" . }} has been down for more than 2 minutes."
{{- end }}
