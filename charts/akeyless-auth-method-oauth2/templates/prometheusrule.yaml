{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-auth-method-oauth2.fullname" . }}
  labels:
    {{- include "akeyless-auth-method-oauth2.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-auth-method-oauth2.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-auth-method-oauth2.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-auth-method-oauth2.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-auth-method-oauth2.fullname" . }} is down"
            description: "{{ include "akeyless-auth-method-oauth2.fullname" . }} has been down for more than 2 minutes."
{{- end }}
