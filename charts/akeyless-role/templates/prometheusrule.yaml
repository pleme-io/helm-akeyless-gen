{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-role.fullname" . }}
  labels:
    {{- include "akeyless-role.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-role.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-role.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-role.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-role.fullname" . }} is down"
            description: "{{ include "akeyless-role.fullname" . }} has been down for more than 2 minutes."
{{- end }}
