{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-policy.fullname" . }}
  labels:
    {{- include "akeyless-policy.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-policy.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-policy.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-policy.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-policy.fullname" . }} is down"
            description: "{{ include "akeyless-policy.fullname" . }} has been down for more than 2 minutes."
{{- end }}
