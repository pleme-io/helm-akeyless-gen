{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-target-gke.fullname" . }}
  labels:
    {{- include "akeyless-target-gke.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-target-gke.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-target-gke.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-target-gke.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-target-gke.fullname" . }} is down"
            description: "{{ include "akeyless-target-gke.fullname" . }} has been down for more than 2 minutes."
{{- end }}
