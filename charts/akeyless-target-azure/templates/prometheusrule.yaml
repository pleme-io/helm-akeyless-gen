{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-target-azure.fullname" . }}
  labels:
    {{- include "akeyless-target-azure.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-target-azure.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-target-azure.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-target-azure.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-target-azure.fullname" . }} is down"
            description: "{{ include "akeyless-target-azure.fullname" . }} has been down for more than 2 minutes."
{{- end }}
