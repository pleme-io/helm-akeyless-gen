{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-classic-key.fullname" . }}
  labels:
    {{- include "akeyless-classic-key.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-classic-key.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-classic-key.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-classic-key.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-classic-key.fullname" . }} is down"
            description: "{{ include "akeyless-classic-key.fullname" . }} has been down for more than 2 minutes."
{{- end }}
