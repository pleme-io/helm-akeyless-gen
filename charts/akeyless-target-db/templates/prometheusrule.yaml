{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-target-db.fullname" . }}
  labels:
    {{- include "akeyless-target-db.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-target-db.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-target-db.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-target-db.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-target-db.fullname" . }} is down"
            description: "{{ include "akeyless-target-db.fullname" . }} has been down for more than 2 minutes."
{{- end }}
