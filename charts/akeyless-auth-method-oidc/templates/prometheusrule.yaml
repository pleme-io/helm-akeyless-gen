{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-auth-method-oidc.fullname" . }}
  labels:
    {{- include "akeyless-auth-method-oidc.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-auth-method-oidc.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-auth-method-oidc.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-auth-method-oidc.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-auth-method-oidc.fullname" . }} is down"
            description: "{{ include "akeyless-auth-method-oidc.fullname" . }} has been down for more than 2 minutes."
{{- end }}
