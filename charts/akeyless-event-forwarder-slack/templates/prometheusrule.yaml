{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-event-forwarder-slack.fullname" . }}
  labels:
    {{- include "akeyless-event-forwarder-slack.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-event-forwarder-slack.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-event-forwarder-slack.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-event-forwarder-slack.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-event-forwarder-slack.fullname" . }} is down"
            description: "{{ include "akeyless-event-forwarder-slack.fullname" . }} has been down for more than 2 minutes."
{{- end }}
