{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-target-ping.fullname" . }}
  labels:
    {{- include "akeyless-target-ping.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-target-ping.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-target-ping.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-target-ping.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-target-ping.fullname" . }} is down"
            description: "{{ include "akeyless-target-ping.fullname" . }} has been down for more than 2 minutes."
{{- end }}
