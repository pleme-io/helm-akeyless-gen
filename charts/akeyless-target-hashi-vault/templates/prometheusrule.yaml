{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-target-hashi-vault.fullname" . }}
  labels:
    {{- include "akeyless-target-hashi-vault.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-target-hashi-vault.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-target-hashi-vault.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-target-hashi-vault.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-target-hashi-vault.fullname" . }} is down"
            description: "{{ include "akeyless-target-hashi-vault.fullname" . }} has been down for more than 2 minutes."
{{- end }}
