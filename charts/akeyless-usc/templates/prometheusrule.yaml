{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-usc.fullname" . }}
  labels:
    {{- include "akeyless-usc.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-usc.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-usc.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-usc.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-usc.fullname" . }} is down"
            description: "{{ include "akeyless-usc.fullname" . }} has been down for more than 2 minutes."
{{- end }}
