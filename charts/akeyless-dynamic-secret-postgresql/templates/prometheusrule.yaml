{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-dynamic-secret-postgresql.fullname" . }}
  labels:
    {{- include "akeyless-dynamic-secret-postgresql.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-dynamic-secret-postgresql.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-dynamic-secret-postgresql.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-dynamic-secret-postgresql.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-dynamic-secret-postgresql.fullname" . }} is down"
            description: "{{ include "akeyless-dynamic-secret-postgresql.fullname" . }} has been down for more than 2 minutes."
{{- end }}
