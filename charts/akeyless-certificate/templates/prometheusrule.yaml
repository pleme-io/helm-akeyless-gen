{{- if .Values.monitoring.alerting.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "akeyless-certificate.fullname" . }}
  labels:
    {{- include "akeyless-certificate.labels" . | nindent 4 }}
spec:
  groups:
    - name: {{ include "akeyless-certificate.fullname" . }}.availability
      interval: 30s
      rules:
        - alert: {{ include "akeyless-certificate.fullname" . | title | replace "-" "" }}Down
          expr: up{{job="{{ include "akeyless-certificate.fullname" . }}"}} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "{{ include "akeyless-certificate.fullname" . }} is down"
            description: "{{ include "akeyless-certificate.fullname" . }} has been down for more than 2 minutes."
{{- end }}
